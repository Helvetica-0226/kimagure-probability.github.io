---
layout: default
title: $f$ , $g$ が可測なら， $f+g$ も可測
permalink: /2/
---

# $f$ , $g$ が可測なら， $f+g$ も可測

 $\mathcal{C} = \{ (-\infty, a] : a \in \mathbb{R} \} $とすると， $\sigma(\mathcal{C})=\mathcal{B}(\mathbb{R})$であることと，<a href="{% link 1.md %}" rel="noopener">1</a>から，任意の $a \in \mathbb{R}$に対して， $B = (\infty, a]$の $f + g$による逆像が $\mathcal{B} (R)$の要素であることを言えばよいです．具体的には以下のように計算します．

$$
\begin{align*}
(f + g)^{-1} (B) &= \{ f(x) + g(x) \le a \} \quad (\mathbb{R} \ni b_n \uparrow a) \\
  &= \bigcup_n [ \{ f(x) \le b_n \} \cap \{ g(x) \le a - b_n \} ] \in \mathcal{B} (X) \\
\end{align*}
$$


[トップに戻る](./)

<script>
  window.MathJax = {
    tex: {
      inlineMath: [['$', '$'], ['\\(', '\\)']],
      displayMath: [['$$', '$$'], ['\\[', '\\]']],
      packages: ['base', 'ams', 'amssymb']
    },
    svg: {
      fontCache: 'global'
    }
  };
</script>
<script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
